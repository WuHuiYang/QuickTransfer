services:
  backend:
    build: ./backend
    container_name: quicktransfer-backend
    restart: always
    volumes:
      - ./data/uploads:/data/uploads
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/quicktransfer.db
      - SECRET_KEY=your-secret-key-change-this-in-production-12345678
      - UPLOAD_DIR=/data/uploads
      - SMTP_HOST=smtp.163.com
      - SMTP_PORT=465
      - SMTP_USER=quicktransfer@163.com
      - SMTP_PASSWORD=UJULmzwkt6mn3VBE
      - SMTP_FROM=quicktransfer@163.com

  frontend:
    build: ./frontend
    container_name: quicktransfer-frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
